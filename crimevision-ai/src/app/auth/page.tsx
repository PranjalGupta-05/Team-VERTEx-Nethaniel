import { AuthContainer } from "@/components/auth/auth-container";

export const metadata = {
  title: "Authentication Portal | VERTEX ID",
  description: "Flagship zero-trust authentication experience with ambient physics and biometrics.",
};

export default function AuthPage() {
  return <AuthContainer />;
}
